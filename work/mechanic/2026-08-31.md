# 2026-08-31 저장소 점검

> 지난주 지적한 poll.sh·work.sh 버그가 모두 고쳐졌고, 새로 썩기 시작한 것도 없습니다 — 이번 주는 깨끗합니다.

## 발견

이번 주 이상 없음.

(액션 버전 노후화는 2026-08-26에 이미 보고했고 아직 해결되지 않았지만, 지난주 보고를 반복하지 않는 원칙에 따라 다시 올리지 않습니다. `actions/checkout@v4`·`setup-node@v4`·`upload-pages-artifact@v3`가 `.github/workflows/_agent.yml:40-44`, `pages.yml:29-36`에 그대로 남아 있습니다.)

## 이상 없던 것

- **README ↔ package.json**: `npm run scout`/`scout:dry`/`test`/`state` 스크립트 4종 모두 README 안내와 일치. `npm test`가 참조하는 `scripts/*.test.mjs` 5개 파일도 실제로 존재.
- **README 구조도 (README.md:29-59)**: `agents/*/{SOUL,TASK,memory}.md`, `scripts/lib/*.mjs` 5종, `scripts/local/*`, `.github/workflows/*` 전부 실제 파일과 일치. 지난주 지적했던 `lib/schedule.mjs`·`lib/output.mjs`·`lib/sections.mjs` 누락은 이미 반영됨.
- **README:126 "책상=Actions든 mini든"**: 08-26에 낡았다고 지적한 문구가 현재는 두 실행 경로를 모두 언급하도록 고쳐져 있음.
- **agents/*/memory.md 상한**: scout 21줄·quill 11줄·curator 24줄·mechanic 13줄 — 전부 30줄 이내. `scripts/lib/memory.mjs`의 `mergeMemory`가 `keep=30`으로 정확히 자르는 것도 코드로 확인.
- **work/ 누적**: scout 10건·quill 3건·mechanic 4건 — 관리 가능한 수준.
- **워크플로**: `pull_request_target` 사용 없음. `permissions`는 README(230-233행)에 적은 대로 에이전트별 최소 권한만 부여(`mechanic.yml`에 `issues: write` 추가, 나머지는 `contents: write`(+`quill`은 `pull-requests: write`))로 실제와 일치.
- **crew.json ↔ 실제 구성**: 등록된 4개 에이전트(scout·quill·curator·mechanic) 모두 `agents/<id>/` 폴더, `.github/workflows/<id>.yml`, `.github/ISSUE_TEMPLATE/<id>.yml`을 빠짐없이 가짐. `output`/`outputFile` 경로(`work/scout`, `work/quill`, `archive/INDEX.md`, `work/mechanic`)는 `run-agent.mjs`·`build-state.mjs`에서 실제로 읽고 쓰는 것을 코드로 확인.
- **scripts/lib 테스트 커버리지**: `memory.mjs`·`output.mjs`·`schedule.mjs`·`state.mjs`·`sections.mjs` 5개 모듈에 대응하는 테스트 파일 5개가 모두 존재.
- **scripts/local/poll.sh, work.sh**: 08-26에 지적한 두 버그 모두 해소 확인 — `poll.sh:51-55`는 잠금-스킵(`RC=75`)일 때 SEEN에 기록하지 않도록 고쳐짐, `poll.sh:34-42`는 모르는 에이전트 라벨에 이슈 댓글을 남기도록 고쳐짐, `work.sh:183-184`는 저장소를 `gh repo view`로 동적으로 가져와 하드코딩이 제거됨.
- **site/office.js ↔ README "숨은 것들" 표**: 파티모드(8초 탭 `ROLLCALL_MS=8000`, 흔들기, 코나미 커맨드), 화분 성장(`localStorage` 저장), 벽시계 클릭, 탕비실 커피 — 전부 코드에 실존하며 문구와 동작이 일치.
